{
  "name": "easy-hevc",
  "version": "1.0.3",
  "description": "Easiest way to convert video files to hevc (H.265) using ffmpeg. ",
  "license": "MIT",
  "author": "imlokesh",
  "type": "module",
  "bin": {
    "easy-hevc": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "bun run index.ts",
    "test": "bun test",
    "build": "bun build index.ts --outfile=dist/index.js --target=node --format=esm",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@imlokesh/clide": "latest"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "@types/bun": "latest",
    "@types/node": "^24.10.13",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/imlokesh/easy-hevc.git"
  }
}
